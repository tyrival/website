<div class="commukit-app" aria-label="CommuKit application interface preview">
  <div class="app-titlebar">
    <div class="traffic-lights" aria-hidden="true"><i></i><i></i><i></i></div>
    <span>CommuKit — Connection Workbench</span>
    <span class="status-light">● Connected</span>
  </div>

  <div class="commukit-tabs">
    <span class="active"><i></i>Modbus Device <b>×</b></span>
    <span>MQTT Broker <b>×</b></span>
    <button aria-label="New session">＋</button>
  </div>

  <div class="commukit-workspace">
    <aside class="connection-sidebar">
      <div class="sidebar-heading"><span>Connections</span><b>＋</b></div>
      <div class="search-field">⌕ &nbsp;Search</div>
      <div class="tree-group"><i></i><strong>Factory Floor</strong><small>3</small></div>
      <div class="tree-row active"><span class="device-dot blue"></span><div><b>Modbus Device</b><small>192.168.1.100:502</small></div></div>
      <div class="tree-row"><span class="device-dot green"></span><div><b>MQTT Broker</b><small>broker.local:1883</small></div></div>
      <div class="tree-row"><span class="device-dot orange"></span><div><b>TCP Server</b><small>0.0.0.0:9000</small></div></div>
      <div class="tree-group tools"><i></i><strong>Tools</strong></div>
      <div class="tool-grid"><span>Ping</span><span>Port Scan</span><span>DNS</span><span>SSL</span></div>
    </aside>

    <section class="message-workspace">
      <div class="connection-bar">
        <div><span class="connected-pill">Connected</span><b>TCP → 192.168.1.100:502</b></div>
        <div class="bar-actions"><span>HEX</span><span>AUTO</span><b>•••</b></div>
      </div>

      <div class="message-stream">
        <div class="message sent"><small>TX · 14:32:01.024</small><code>01 03 00 00 00 02 C4 0B</code></div>
        <div class="message received"><small>RX · 14:32:01.062</small><code>01 03 04 00 64 00 C8 FA 1D</code></div>
        <div class="parser-result">
          <div class="parser-label"><span>✓</span> Parser <small>MODBUS RTU Response</small></div>
          <div class="register"><span>Register[0]</span><b>100</b><code>0x0064</code></div>
          <div class="register"><span>Register[1]</span><b>200</b><code>0x00C8</code></div>
          <div class="crc">CRC-16 &nbsp; 0xFA1D &nbsp; Valid</div>
        </div>
        <div class="message sent"><small>TX · 14:32:05.311</small><code>01 03 00 01 00 01 D5 CA</code></div>
      </div>

      <div class="composer">
        <div class="composer-tools"><span>HEX</span><span>UTF-8</span><span>⌘ Enter to send</span></div>
        <div class="composer-input"><code>01 03 00 02 00 02</code><button aria-label="Send message">↑</button></div>
      </div>
    </section>

    <aside class="session-inspector">
      <div class="workflow-heading">
        <p class="inspector-label">Communication Workflow</p>
        <span>Running</span>
      </div>
      <strong class="workflow-name">Device telemetry handshake</strong>
      <div class="workflow-step complete"><i>✓</i><div><b>Send request</b><span>01 03 00 00 00 02</span></div></div>
      <div class="workflow-line complete"></div>
      <div class="workflow-step complete"><i>✓</i><div><b>Wait for matching reply</b><span>temperature &gt; 20</span></div></div>
      <div class="workflow-line"></div>
      <div class="workflow-step active"><i>3</i><div><b>Parser: Modbus Response</b><span>Extract registers</span></div></div>
      <div class="run-log">
        <span>Run log</span>
        <code>reply.temperature = 24.5</code>
        <code>next.deviceId = A-104</code>
      </div>
    </aside>
  </div>
</div>

<style>
  .commukit-app { min-width: 820px; overflow: hidden; border: 1px solid var(--preview-border); border-radius: 18px; background: var(--preview-bg); color: var(--preview-text); box-shadow: var(--preview-shadow); font-family: Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; }
  .app-titlebar { height: 42px; display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; padding: 0 15px; border-bottom: 1px solid var(--preview-border); color: var(--preview-muted); font-size: 10px; font-weight: 600; }
  .traffic-lights { display: flex; gap: 7px; }.traffic-lights i { width: 11px; height: 11px; border-radius: 50%; background: #ff5f57; }.traffic-lights i:nth-child(2) { background: #febc2e; }.traffic-lights i:nth-child(3) { background: #28c840; }
  .status-light { justify-self: end; color: #31be72; font-size: 9px; }
  .commukit-tabs { height: 34px; display: flex; align-items: end; gap: 2px; padding-left: 180px; border-bottom: 1px solid var(--preview-border); background: var(--preview-canvas); }
  .commukit-tabs > span { height: 30px; min-width: 132px; display: flex; align-items: center; gap: 7px; padding: 0 10px; border: 1px solid transparent; border-bottom: 0; color: var(--preview-faint); font-size: 9px; }
  .commukit-tabs > span.active { border-color: var(--preview-border); border-radius: 7px 7px 0 0; background: var(--preview-bg); color: var(--preview-text); }
  .commukit-tabs span i { width: 6px; height: 6px; border-radius: 50%; background: #31be72; }.commukit-tabs span b { margin-left: auto; font-weight: 400; }.commukit-tabs button { width: 29px; height: 29px; border: 0; background: transparent; color: var(--preview-faint); }
  .commukit-workspace { height: 390px; display: grid; grid-template-columns: 180px minmax(380px, 1fr) 185px; background: var(--preview-bg); }
  .connection-sidebar { padding: 12px 9px; border-right: 1px solid var(--preview-border); background: var(--preview-panel); }
  .sidebar-heading { display: flex; justify-content: space-between; padding: 0 6px 9px; color: var(--preview-faint); font-size: 9px; font-weight: 750; letter-spacing: .12em; text-transform: uppercase; }.sidebar-heading b { color: var(--accent); font-size: 14px; }
  .search-field { height: 27px; display: flex; align-items: center; margin-bottom: 9px; padding: 0 8px; border: 1px solid var(--preview-border); border-radius: 7px; color: var(--preview-faint); font-size: 9px; }
  .tree-group { display: flex; align-items: center; gap: 6px; padding: 8px 5px 5px; font-size: 9px; }.tree-group i { width: 6px; height: 6px; border: solid var(--preview-faint); border-width: 0 1px 1px 0; transform: rotate(45deg); }.tree-group small { margin-left: auto; color: var(--preview-faint); }
  .tree-row { min-height: 42px; display: flex; align-items: center; gap: 8px; margin: 2px 0; padding: 6px 7px; border: 1px solid transparent; border-radius: 8px; }.tree-row.active { border-color: color-mix(in srgb, var(--accent) 20%, transparent); background: color-mix(in srgb, var(--accent) 9%, transparent); }.device-dot { width: 7px; height: 7px; border-radius: 50%; }.device-dot.blue { background: var(--accent); }.device-dot.green { background: #31be72; }.device-dot.orange { background: #ff9f0a; }.tree-row div { min-width: 0; }.tree-row b, .tree-row small { display: block; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }.tree-row b { font-size: 9px; }.tree-row small { margin-top: 3px; color: var(--preview-faint); font-size: 7px; }.tools { margin-top: 12px; border-top: 1px solid var(--preview-border); }.tool-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 4px; }.tool-grid span { padding: 6px; border: 1px solid var(--preview-border); border-radius: 6px; color: var(--preview-muted); font-size: 8px; text-align: center; }
  .message-workspace { min-width: 0; display: flex; flex-direction: column; background: var(--preview-canvas); }
  .connection-bar { height: 40px; display: flex; align-items: center; justify-content: space-between; padding: 0 13px; border-bottom: 1px solid var(--preview-border); background: var(--preview-panel); }.connection-bar > div { display: flex; align-items: center; gap: 8px; }.connection-bar b { font-size: 8px; }.connected-pill { padding: 3px 6px; border-radius: 10px; background: color-mix(in srgb, #31be72 12%, transparent); color: #31be72; font-size: 7px; font-weight: 700; }.bar-actions span { padding: 3px 5px; border: 1px solid var(--preview-border); border-radius: 4px; color: var(--preview-faint); font-size: 7px; }
  .message-stream { flex: 1; display: flex; flex-direction: column; gap: 10px; padding: 12px; overflow: hidden; }
  .message { display: flex; flex-direction: column; align-items: flex-start; }.message.sent { align-items: flex-end; }.message small { margin: 0 4px 3px; color: var(--preview-faint); font-size: 7px; }.message code { padding: 8px 11px; border: 1px solid var(--preview-border); border-radius: 12px 12px 12px 4px; background: var(--preview-panel-strong); color: var(--preview-text); font-size: 9px; }.message.sent code { border-color: var(--accent); border-radius: 12px 12px 4px 12px; background: var(--accent); color: white; }
  .parser-result { width: 77%; padding: 9px 10px; border: 1px solid color-mix(in srgb, #31be72 25%, transparent); border-radius: 11px; background: color-mix(in srgb, #31be72 6%, var(--preview-panel)); }.parser-label { display: flex; align-items: center; gap: 5px; margin-bottom: 6px; color: #31be72; font-size: 8px; font-weight: 700; }.parser-label small { margin-left: auto; color: var(--preview-faint); font-weight: 400; }.register { display: grid; grid-template-columns: 1fr 38px 55px; padding: 3px 0; border-top: 1px solid color-mix(in srgb, #31be72 13%, transparent); font-size: 8px; }.register b { color: var(--accent); }.register code { color: var(--preview-faint); }.crc { margin-top: 4px; color: #31be72; font: 7px "JetBrains Mono", monospace; }
  .composer { padding: 8px 11px 11px; border-top: 1px solid var(--preview-border); background: var(--preview-panel); }.composer-tools { display: flex; gap: 5px; margin-bottom: 5px; color: var(--preview-faint); font-size: 7px; }.composer-tools span:last-child { margin-left: auto; }.composer-input { height: 34px; display: flex; align-items: center; padding-left: 10px; border: 1px solid var(--preview-border); border-radius: 9px; background: var(--preview-canvas); }.composer-input code { flex: 1; color: var(--preview-muted); font-size: 9px; }.composer-input button { width: 25px; height: 25px; margin-right: 4px; border: 0; border-radius: 7px; background: var(--accent); color: white; }
  .session-inspector { padding: 13px; border-left: 1px solid var(--preview-border); background: var(--preview-panel); }.inspector-label { margin: 0; color: var(--preview-faint); font-size: 8px; font-weight: 750; letter-spacing: .08em; text-transform: uppercase; }.workflow-heading { display: flex; align-items: center; justify-content: space-between; gap: 5px; }.workflow-heading > span { padding: 3px 5px; border-radius: 8px; background: color-mix(in srgb, #31be72 12%, transparent); color: #31be72; font-size: 7px; font-weight: 700; }.workflow-name { display: block; margin: 8px 0 14px; font-size: 9px; }.workflow-step { display: flex; align-items: center; gap: 7px; }.workflow-step i { width: 20px; height: 20px; flex: 0 0 20px; display: grid; place-items: center; border: 1px solid var(--preview-border); border-radius: 50%; color: var(--preview-faint); font-size: 8px; font-style: normal; }.workflow-step.complete i { border-color: color-mix(in srgb, #31be72 35%, transparent); background: color-mix(in srgb, #31be72 8%, transparent); color: #31be72; }.workflow-step.active i { border-color: color-mix(in srgb, var(--accent) 40%, transparent); background: color-mix(in srgb, var(--accent) 9%, transparent); color: var(--accent); }.workflow-step b, .workflow-step span { display: block; }.workflow-step b { font-size: 7px; line-height: 1.25; }.workflow-step span { margin-top: 2px; overflow: hidden; color: var(--preview-faint); font: 6px "JetBrains Mono", monospace; text-overflow: ellipsis; white-space: nowrap; }.workflow-line { width: 1px; height: 15px; margin-left: 10px; background: var(--preview-border); }.workflow-line.complete { background: color-mix(in srgb, #31be72 35%, var(--preview-border)); }.run-log { margin-top: 16px; padding: 8px; border: 1px solid color-mix(in srgb, var(--accent) 18%, var(--preview-border)); border-radius: 8px; background: color-mix(in srgb, var(--accent) 4%, var(--preview-canvas)); }.run-log span, .run-log code { display: block; }.run-log span { margin-bottom: 5px; color: var(--accent); font-size: 7px; font-weight: 750; text-transform: uppercase; }.run-log code { overflow: hidden; color: var(--preview-faint); font-size: 6px; line-height: 1.7; text-overflow: ellipsis; white-space: nowrap; }
  @media (max-width: 980px) { .commukit-workspace { grid-template-columns: 160px minmax(380px, 1fr); } .session-inspector { display: none; } .commukit-tabs { padding-left: 160px; } }
</style>
